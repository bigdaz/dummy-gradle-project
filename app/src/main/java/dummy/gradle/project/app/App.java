/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dummy.gradle.project.app;

import dummy.gradle.project.list.LinkedList;

import static dummy.gradle.project.utilities.StringUtils.join;
import static dummy.gradle.project.utilities.StringUtils.split;
import static dummy.gradle.project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
